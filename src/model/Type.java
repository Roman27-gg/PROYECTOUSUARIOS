package model;

/**
 * Representa los tipos de usuario dentro del sistema.
 */
public enum Type {
     /** Usuario con permisos completos para administrar el sistema. */
     ADMIN, 
     
     /** Usuario estándar con permisos limitados. */
     STANDARD
}
